{
  "name": "crispy-crypto-market-maker",
  "private": true,
  "version": "0.2.0",
  "packageManager": "pnpm@10.18.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "check:lines": "./scripts/check-file-lines.sh"
  },
  "devDependencies": {
    "turbo": "2.5.8"
  }
}
