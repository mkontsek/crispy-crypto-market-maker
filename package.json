{
  "name": "crispy-crypto-market-maker",
  "private": true,
  "version": "0.2.0",
  "packageManager": "pnpm@10.18.0",
  "scripts": {
    "dev": "bash ./scripts/free-dev-ports.sh 3008 && turbo run dev --parallel",
    "dev:exchange": "bash ./scripts/free-dev-ports.sh 3111 && cd apps/exchange && cargo watch -x run",
    "dev:bot": "bash ./scripts/free-dev-ports.sh 3110 && cd apps/bot && cargo watch -x run",
    "dev:web": "bash ./scripts/free-dev-ports.sh 3008 && pnpm --filter @crispy/web dev",
    "dev:all": "pnpm run dev:exchange & pnpm run dev:bot & pnpm run dev:web",
    "build": "turbo run build",
    "build:all": "pnpm run build:exchange && pnpm run build:bot && pnpm --filter @crispy/web build",
    "build:exchange": "cd apps/exchange && cargo build --release",
    "build:bot": "cd apps/bot && cargo build --release",
    "lint": "turbo run lint",
    "lint:rust": "cd apps/exchange && cargo clippy -- -W clippy::pedantic && cd ../bot && cargo clippy -- -W clippy::pedantic",
    "lint:all": "pnpm run lint && pnpm run lint:rust && pnpm run check:lines",
    "typecheck": "turbo run typecheck",
    "check:lines": "./scripts/check-file-lines.sh",
    "test:web": "turbo run test",
    "test:exchange": "cd apps/exchange && cargo test",
    "test:bot": "cd apps/bot && cargo test",
    "test:rust": "pnpm run test:exchange && pnpm run test:bot",
    "test": "pnpm run test:web && pnpm run test:rust",
    "check:precommit": "pnpm lint:all && pnpm test && pnpm build:all",
    "db:up": "docker compose up -d --wait postgres && DATABASE_URL=\"postgresql://postgres:postgres@localhost:${POSTGRES_HOST_PORT:-55432}/postgres\" pnpm --filter @crispy/db prisma:push",
    "hooks:install": "node ./scripts/install-git-hooks.js",
    "prepare": "node ./scripts/install-git-hooks.js"
  },
  "devDependencies": {
    "turbo": "2.8.15"
  }
}
